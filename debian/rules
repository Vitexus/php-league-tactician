#!/usr/bin/make -f
PKG_VERSION := $(shell dpkg-parsechangelog -S Version | sed 's/~.*//')
PKG_SOURCE  := $(shell jq -r '.name' composer.json)

%:
	dh $@

override_dh_install:
	mkdir -p debian/php-league-tactician/usr/share/php/League/Tactician
	cp -r src/. debian/php-league-tactician/usr/share/php/League/Tactician/
	install -m 644 debian/autoload.php debian/php-league-tactician/usr/share/php/League/Tactician/autoload.php
	sed -i "s|'unknown'|'$(PKG_SOURCE)'|" debian/php-league-tactician/usr/share/php/League/Tactician/autoload.php
	sed -i "s|'0.0.0'|'$(PKG_VERSION)'|" debian/php-league-tactician/usr/share/php/League/Tactician/autoload.php
